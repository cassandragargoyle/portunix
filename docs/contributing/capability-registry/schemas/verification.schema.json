{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "verification.schema.json",
  "title": "Verification",
  "description": "Evidence and verification state, mandatory on every node and relation.",
  "type": "object",
  "required": ["status", "lastChecked"],
  "additionalProperties": false,
  "properties": {
    "status": {
      "description": "How well the information is verified.",
      "enum": ["declared", "documented", "tested", "production", "deprecated", "unknown"]
    },
    "source": {
      "type": "string",
      "description": "Origin of the claim (vendor-site, internal-test, doc URL, ...)."
    },
    "lastChecked": {
      "type": "string",
      "format": "date",
      "description": "YYYY-MM-DD the information was last verified."
    },
    "evidence": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Paths or URLs to supporting evidence."
    }
  }
}
